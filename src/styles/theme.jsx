import { extendTheme } from '@chakra-ui/react';

const theme = extendTheme({
  fonts: {
    body: "Poppins, sans-serif", // Body text font family
    heading: "Poppins, sans-serif", // Heading font family
  },
});

export default theme;